apiVersion: v1
kind: Service
metadata:
  name: {{ include "greenbelt-static-image-generator-server.fullname" . }}
  labels:
    {{- include "greenbelt-static-image-generator-server.labels" . | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "greenbelt-static-image-generator-server.selectorLabels" . | nindent 4 }}
